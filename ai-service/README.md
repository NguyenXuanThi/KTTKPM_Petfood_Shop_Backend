# AI Service

Pet shop AI chatbot (Groq + Socket.IO). Syncs product catalog via Kafka/HTTP fallback.

## Run

```bash
npm install
npm run dev
```

Default port: **3011** (`AI_PORT` or legacy `CHAT_PORT`).

## Env

See `.env.example`.
